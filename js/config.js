/**
 * Site Configuration
 *
 * Feature flags and global settings
 */

export const config = {
    // Set to 'resume' to show resume view, 'home' for normal site
    viewMode: 'resume', // 'home' | 'resume'
};
